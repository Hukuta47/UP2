<Page x:Class="RepairTrack.Pages.Roles.Manager.DashboardManagerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:RepairTrack.Pages.Roles.Manager"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DashboardManagerPage">

	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="9*"/>
			<RowDefinition/>
		</Grid.RowDefinitions>

		<DataGrid Name="DataGrid_RequestList" AutoGenerateColumns="False" IsReadOnly="True">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Дата создания заявки" Binding="{Binding DateAdded, StringFormat=dd.MM.yy}"/>
				<DataGridTextColumn Header="Клиент" Binding="{Binding Users.Name}"/>
				<DataGridTextColumn Header="Тип устройства" Binding="{Binding Issue.Name}"/>
				<DataGridTextColumn Header="Проблема" Binding="{Binding Deadline, StringFormat=dd.MM.yy}"/>
				<DataGridTemplateColumn>
					<!--Кнопка просмотра-->
					<DataGridTemplateColumn.CellTemplate>
						<DataTemplate>
							<Button>
								<TextBlock Text="Указать техника"/>
							</Button>
						</DataTemplate>
					</DataGridTemplateColumn.CellTemplate>
				</DataGridTemplateColumn>
			</DataGrid.Columns>
		</DataGrid>

		<StackPanel Grid.Row="1" Orientation="Horizontal">
			<Button Content="Отчет"/>
		</StackPanel>

	</Grid>
</Page>
